- Kevin
	- At S1 
		- cannot determine canopy reflectance with browning etc
		- continual coverage is great
	- At S2 
		- see the browning 
	- Infusing s2 where available
		- pain in the ass to do
		- contrastive learning between s1 and s2
			- foundation models designed to infer details about others 
			- encode the text with one model, image with one model
				- these refer to the same thing
				- once repeated enough the model would be able to generate the new image
					- train and encoder to understand when the s1 and s2 are aligned
					- only giving information about s1, but can infer details about s2 from the encoder 
						- "observe" browning through the canopy decay
		- do not overcomplicate with attenion
			- CLIP representation learning (understanding)
			- CROMA (good implementation)
				- encoder for s1 chroma
		- pre-trained models with great documentation on implementation
			- https://pytorch.org/blog/geospatial-deep-learning-with-torchgeo/
	- Data Collection
		- fetching the entire scene from the url is the bottle neck 
			- single scene contains multiple tiles
			- clustering algoritm of lat lon of interest (what are my nearest lat lon that are together)
			- See if there are any way to fetch other scenes nearby 
		- look at what the scene would be for each island
			- is the entire island contained in each scene?
				- how big is the scene am I getting back
				- work smarter not harder
				- what stuff can you cache (minor caching )
					- this takes a long time, here are my bottlenecks, what can I reuse at these given bottlenecks
				- 16 cores on sever (scala is the production grade for parallelization, apache spark)
					- single task looking up scene (no parallelize)
						- once scene is found parallel tile request
					- one scene gives # tiles
						- parallel lookup scene so that you can lookup tiles too
		- mark zhao (facebook data collection)
		- there should be an efficient api that will collection the scenes for you given a geojson masterlist 