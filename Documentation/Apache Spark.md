parallelize the geotif download with scenes
- how many tiles per scene with number of workers one worker per task 
- always limited by the number of tiles given for the workers 
- pyspark orchestration software airflow, visualize and run the pipeline
	- debug the etl
	- seeder for ml
