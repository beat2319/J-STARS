## Jen
disability check for ui designer contrast checker
- tagged labels etc
- font and constrasts
- WCAGS acessabiity 
https://github.com/andypbarrett/sentinel1denoised
https://github.com/andypbarrett/sentinel1_search
Look into denoising algorithms 
- similar to Barlow twins but not barlow twins
- look into cotranstive learning, but not traditional 
	- looking at a noisy image, then use an image but isolating the noise
- use the existing models

## Zhongying
First do a time series plot of both datasets
- then do the spatial or temporal autocorelaltion 
- maybe focus more on time as the spatial patterns are noisy 
- balance between model performance, 
	- two stage model processing steps
	- 1 for each of the timesteps conv backbone then gen embeddings
		- parallezized 
	- lstms is waitinf for all of those convulatiosn to be done 
		- not paraelelized 
	- do not keep addign timesteps, is it worth it or not
		- significant improvement 
	- you must make quick predictions
		- you cannot train a huge model 
How to measure spatial autocorrelation based on labels 
- maybe just based on input 
	- how does the pixel based on the time or space
- plot the label over or 
Downloading
- Look into existinf GEE or collection pipelines that others have used 

Conv-LSTM (both with and without UNet)
- lets see how the just conv-lstm preforms 
	- look into the time
	- 2 - 3 layers max just take it easy
- With Unet
	- only used in downsampling
	- regular unet for the upsampling
		- look into existinfg github repo with exisuting convlstm unet

Just focus on sentiel-1 (not really a point if using AlphaEarth Embeddings)
- dataset for harmonized landsat and sentiel-2 (newer version online, both EE and hugging face)
	- chozen highest resolution
	- combined s2 and landsat
	- they are complementary sources

AlphaEarth use the RGB pretrained
- same resolution at s1 and s2 (10 x 10m)
- less important than intial iinput 
- use the current year alpha earth embedding subnetwork that is fused with the unet